var a,b,c;   // 1)

// Legal
var num,x,y,z,a;   // 2)
// Illegal
//  var -num, @myvar, *mynum, ?z, %s;

document.write("<h1>Rules for naming JS variables</h1>"); //  3)
document.write("Variable names can only contain numbers, $ and _ .For example $my_1stVariable <br> Variables must begin with a letter, $ _. For example $name, _name or name <br> Variable names are case sensitive <br> Variable names should not be JS Keyword");